export enum ThemeMode {
    LIGHT = 'light',
    DARK = 'dark',
}

export enum AuthMode {
    LOGIN = '로그인',
    SIGNUP = '회원가입',
    AUTH = '인증',
}

export type ModeType = 'light' | 'dark';

export type AuthModeType = '로그인' | '회원가입' | '인증';
